{
  "name": "bnb",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ drbh/bnb",
    "create-local": "graph create --node http://localhost:8020/ drbh/bnb",
    "remove-local": "graph remove --node http://localhost:8020/ drbh/bnb",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 drbh/bnb"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.10.0",
    "@graphprotocol/graph-ts": "0.6.0"
  }
}
